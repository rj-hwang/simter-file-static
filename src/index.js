// 引入另一个组件
import main from './main';
import Vue from 'vue';

main();

vueP.then(function(Vue) {
  new Vue({
    el: "#app",
    data: {
      name: "Simter File Server For Static Files"
    }
  });
});